{
  "name": "ai-gftd-dougaka-vector",
  "private": true,
  "description": "Language/character-independent vector-animation video workflow (storyboard EDN -> SVG frames)",
  "scripts": {
    "test": "nbb --classpath src:test test/run.cljs",
    "render:example": "nbb --classpath src bin/render.cljs examples/quantization.edn --out /tmp/dougaka-vector/quantization"
  },
  "optionalDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
